# Inyo P2 质量加固实施记录

> 目标：在保留 P1 修复、`demo-shots/` 和用户工作区修改的前提下，补齐颜色 token、Hero 资源范围、a11y/i18n 生成物门禁与 CI 发布门禁。

## 约束

- 运行时颜色来自 `assets/css/main.css` token；`static/img/seal-yang.svg` 仅作为无 CSS 变量上下文的 favicon 固定品牌色例外。
- Hero 交互继续受首页、`hover:hover` 和 `prefers-reduced-motion` 约束。
- 不引入前端框架、CSS 预处理器、axe/Jest 或运行时依赖。
- 不修改或整理 `demo-shots/`，不新增内容功能，不自动提交或创建 PR。

## 实施任务

- [x] 扩展 `scripts/verify-theme.ps1`：颜色 token、favicon 例外、首页 Hero、页面 a11y、外链安全、图片 alt、无 `vertical-rl`、三语必需 key。
- [x] 将网页内联 Logo 改为 `--seal-disc` / `--seal-ink` / `--seal-light` / `--cinnabar`，并将印章阴影与打印背景改为 token。
- [x] 只在 `.IsHome` 时渲染 `inky-overlay.html`，同步 README、DESIGN 和主题指南。
- [x] 创建 `.github/workflows/verify-theme.yml`：Windows runner、Go `go.mod`、Hugo Extended `0.164.0`、构建/smoke/diff。
- [x] 保留现有三语 key 与模板实现，并用生成物断言固化其合同；未对已满足的 i18n 文案做无意义改写。

## 验证记录

- `hugo --source exampleSite --minify`：通过，38 pages / 196 static files / 1 processed image。
- `pwsh -File scripts/verify-theme.ps1`：通过，输出 `P1/P2 smoke checks passed.`。
- `git diff --check`：通过；工作区仅有 Git 的 LF/CRLF 提示，无 whitespace error。
- Headless Chrome DOM：通过，首页包含 Hero，文章页不包含 Hero 输出。
- 只读代码 reviewer：P2 范围无 P0–P3 可行动问题。

