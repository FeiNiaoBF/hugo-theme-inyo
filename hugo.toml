# Inyo 陰陽 theme metadata
name = "Inyo"
description = "纸墨二元 · 日式现代简约 × 古中国水墨 — Hugo 博客主题"
license = "MIT"
min_version = "0.164.0"

# Runtime defaults are provided through config/_default/*.toml so Hugo Modules
# can merge them with the consuming site's project configuration.

[module.hugoVersion]
extended = true
min = "0.164.0"
