# Inyo 陰陽 · 设计文档 (DESIGN.md)

> 纸墨二元 · 日式现代简约 × 古中国水墨 · Hugo 博客主题
> 文档状态：持续维护 — 2026-08-15（纸墨体系、目录信息架构与诗句 Hero）

## 0. 主题身份

- **名称**: Inyo（陰陽）— 阴阳，纸墨二元性
- **机制**: 亮色模式 = 墨在纸上（荼蘼白底 + 墨字）；暗色模式 = 白墨在墨（墨色底 + 月白字）
- **目标**: 长文阅读优先的小众审美博客主题；日式现代简约的秩序，古中国水墨的墨韵
- **技术**: Hugo（Hugo Modules 分发），无 JavaScript 运行时依赖；主题切换与首页诗词交互使用少量原生内联脚本，样式由 CSS 变量 token 管理

## 1. 设计原则

1. **墨韵为骨**：全站只有一个情绪色——朱砂红。60/25/10/5 用色比例：底色 60% / 墨文 25% / 朱砂 10% / 昭和暖 5%
2. **留白为呼吸**：间（ma）——页首尾大留白，容器 40em，正文行高 2.0
3. **数据为证**：每个颜色 token 有双重背书——742 中华传统色库点名（真实色名）+ WCAG 对比度实测（≥4.5:1）

## 2. 配色系统

### 2.1 亮色模式（纸）

| 角色 | 色名 | HEX | 来源(742库) | 实测对比度 |
|---|---|---|---|---|
| 底色 | 荼蘼白 | `#F8F4F0` | 30号 | — |
| 辅助面(卡片/引用) | 蚌肉白 | `#F9F1DB` | 53号 | vs 底 1.01:1 安静分层 |
| 正文 | 墨色 | `#1D1B1C` | 墨色 | **15.66:1** |
| 链接/印章 | 朱砂红 | `#D92121` | 朱砂红 | **4.58:1** |
| 抬升面链接 / 代码关键字 | 鹤顶红 | `#D42517` | 鹤顶红 | on `#F9F1DB` **4.571:1** |
| 标签/时间戳 | 驼色 | `#66462A` | 驼色 | **7.76:1** |
| 代码 | 黛蓝 | `#2A3C5C` | 黛蓝 | **10.10:1** |
| 装饰暖(大文本) | 琥珀 | `#CA6924` | 琥珀 | 3.45:1 仅 ≥3:1 大文本 |
| 表面层级 | OKLCH 插值 | `#E6E4DF` `#CCCDC8` `#989D9B` `#6A6E71` `#4D4E52` | 算法生成 | — |

### 2.2 暗色模式（墨）— 墨色基（ADR-7，取代 ADR-2 剑锋紫）

| 角色 | 色名 | HEX | 来源(742库) | 实测对比度 |
|---|---|---|---|---|
| 底色 | 墨色 | `#1D1B1C` | 墨色 | —（= 亮色正文墨色，纸墨闭环） |
| 正文 | 月白暖灰 | `#ECE6DC` | 暖白系（算法取色） | **13.80:1** |
| 链接/印章 | 朱红 | `#ED5126` | 朱砂红系提亮（兜底标注） | **4.74:1** |
| 抬升面链接 / 代码关键字 | 舌红 | `#F19790` | 舌红 | on `#322F31` **6.032:1**；on `#2C2C2C` **6.364:1** |
| 标签/日期 | 晓灰 | `#D4C4B7` | 晓灰 | **10.10:1** |
| 代码文字 | 云峰灰 | `#C1C8D6` | 云峰灰 | **10.20:1** |
| 按钮 | 朱红底 + 墨色字 | `#ED5126`/`#1D1B1C` | — | **4.74:1** |
| 代码块底 | 缁 | `#2C2C2C` | 缁 | 云峰灰 on 缁 **8.31:1** |
| 抬升面(卡片/引用) | 墨色→月白插值 | `#322F31` | 算法生成 | 见 2.3 纪律2 |
| 表面层级 5 档 | 墨色→月白短路径插值 | `#262324` `#322F31` `#4A4648` `#888185` `#C0B6BB` | 算法生成 | 见 2.3 |

### 2.3 三条使用纪律（实测数据，必须遵守）

1. **墨色 `#1D1B1C` 上正文/链接全角色过 AA**——月白 13.80、朱红 4.74、晓灰 10.10。原“松烟墨禁作剑锋紫辅助面”条款随剑锋紫体系删除
2. **抬升面链接与代码关键字使用专用角色**——亮色 `--link-raised` / `--syntax-keyword` 使用鹤顶红 `#D42517`，on `#F9F1DB` 为 4.571:1；暗色使用舌红 `#F19790`，on `#322F31` 为 6.032:1、on `#2C2C2C` 为 6.364:1。普通纸面链接继续使用 `--cinnabar`
3. **t≥0.55 浅档（`#888185` 及以上）禁止放正文/链接**——只作弱化文字（on 底 4.51:1）、边框或装饰

### 2.4 色相和谐结构（OKLCH 感知色相轮）

- 暖色族：朱砂红 27.6° · 野蔷薇红 46.7° · 琥珀 51.2° · 驼色 61.2° —— 邻近关系，同一温度轴
- **朱砂红 ↔ 黛蓝：ΔH=126.3° = 三角关系（triadic）**——经典稳定结构
- 全盘唯一宽容差：朱砂红↔驼色 ΔH=33.6°（严格模板外 3.6°，色温轴相同，接受）
- 暗色朱红 `#ED5126` 与亮色朱砂红 `#D92121` 同温度轴（暖红系）——双模式朱砂统一，不再有 v1 剑锋紫与暖纸的温度轴分离

## 3. 字体系统

| 角色 | 字体栈 | 参数 |
|---|---|---|
| 中文正文 | `'LXGW WenKai','Noto Serif SC','Source Han Serif SC','KaiTi',serif` | 默认 **霞鹜文楷**（OFL 1.1，**自托管分片**：static/fonts/wenkai + assets/css/wenkai.css）；16px · 行高 **2.0** |
| 标题 | 同族加粗（霞鹜文楷 Bold） | h1 字距 **1px** · h2 **0.75px** |
| 思源宋体（可选） | `'Noto Serif SC','Source Han Serif SC','SimSun',Georgia,serif` | `params.font = "serif"` |
| 代码/元信息 | Sarasa Mono SC 更纱黑体 | 13px · 行高 1.6 · 日期/阅读时间/版权 |
| 容器 | — | **40em**（中文一行 30–45 字舒适区） |

## 4. 组件风格

| 组件 | 规范 |
|---|---|
| Logo | **双鱼太极 + 朱砂环**（v4，2026-08-06 选定）：网页内联 Logo 使用 `--seal-disc` / `--seal-ink` / `--seal-light` 主题 token + `--cinnabar`，亮暗模式自适应；favicon 因无 CSS 变量上下文继续使用品牌固定色版 `img/seal-yang.svg`（唯一固定色例外） |
| 分隔线 | 已删除（v2）——目录用留白 |
| 竖排题款 | 已删除（v2）——正文、导航、页脚一律横排 LTR |
| 顶部导航 | 已移除（v4，并入右侧身份区横排文字的纵向堆叠导航） |
| 身份区 | 太极 Logo + 站名（桌面 36px；平板 28px；手机 24px）组成品牌行，气质签名（`params.subtitle`）在下一行与站名文字左轴锁定；长站名自然换行。桌面为右侧纵向 rail，≤768px 为顶部品牌区；导航与语言/☯ 组成同一工具栏，激活项亮暗统一使用颜色 + 常驻下划线，≤1000px 下划线收至 2px；短视口下工具栏在 Footer 上方独立滚动 |
| 页脚 | 单一语义 `<footer>`，DOM 位于正文之后；桌面、平板与移动端均为全宽、文档流内的页面页脚（顶部 `--border` 分隔），社交链接与版权横排，`/` 分隔；不再固定于右侧 rail 底部 |
| 卡片/引用 | 蚌肉白底 + 朱砂左竖线 · 直角 2px；内部链接使用 `--link-raised` |
| 表格 | 仅横线（中式简净），无竖线 |
| 标签 | 弱驼色描边小章（驼色 45% 的 1px 边框 + 极小字距），避免与文章标题争夺层级 |
| 按钮 | 朱砂红底 + 荼蘼白字（亮暗一致） |
| 文字选中 | 朱砂红 15% 透明度晕染 |
| 代码块 | Hugo Chroma `noClasses = false` 输出语义 class；亮:蚌肉白底墨字 / 暗:缁底云峰灰字，语法角色走 CSS token（注释/标题=驼色，关键字=`--syntax-keyword`，字符串=黛蓝） |

## 5. 排版参数（有真实主题背书）

- 正文 15px / 行高 2.0 / 字距 0.3px —— 对齐水墨主题实测值（markdown.scss）
- 标题字距拉开（2px/1.5px）—— 东方排版气质来源
- 容器 42em —— CJK 阅读舒适区（一行约 30–45 字）；扣除容器内边距后正文实际宽度约 632px
- 桌面阅读轴 —— `.content` 使用 `2vw` 左内缩，让首页目录、列表页、诗句 Hero 与文章正文共享轻微左偏的安静轴线；42em 阅读分块保持克制扩宽
- 数学公式保留 KaTeX 自带字体（Computer Modern 风格）——等宽无衬线缺斜体变量，不适合数学排版

## 6. 首页：目录（v3 — pianpker 式）

- **结构**: 桌面采用右侧固定身份区（18em，pianpker 实测：大字站名 36px + 副标语 + 横排文字的纵向堆叠导航）+ 左内容区 42em；页脚语义上位于正文之后，视觉上为全宽文档流页面页脚
- **首页 = 目录**: 先展示 `pinned = true` 的置顶文章，再按日期分组展示最新文章（`GroupByDate "2006-01-02" "desc"`）；每组文章标题 + 日期 + 阅读时间（更纱黑体）；摘要优先使用 Hugo `.Summary`，再做宽度感知截断（CJK=2/ASCII=1，140 单位）并限制为 3 行
- **标题字重**: 目录标题常规字重（400），对齐 pianpker 阅读气质
- **内容状态**: 零篇本地化空状态
- **响应层级**: ≤480px 使用 24px 站名与紧凑横向工具栏；481–768px 使用 28px 站名，并以 `max(1.25em, calc((100% - var(--container)) / 2 + 1.25em))` 让品牌、工具栏、Hero 和正文共享同一左轴；769–1000px 保留 14em 右侧 rail 并收紧导航间距；>1000px 保留 18em rail、42em 内容块和 `2vw` 阅读轴
- **移动构图**: ≤768px 身份区高度保持 14em，工具栏落在品牌区下缘，Hero 顶部约 248px，继续作为首个三分线附近的过渡锚点；页脚在正文之后，不再占据身份区高度
- **禁止项**: 无卡片、阴影、封面图、标签堆叠和首页分页；年份分组只用于独立的 `/archives/` 归档页

### 6.1 博客信息架构

- 主导航角色固定为：首页、博客、标签、归档、关于；默认路径分别是 `/`、`/posts/`、`/tags/`、`/archives/`、`/about/`。
- 实际路径由 `params.mainSections[0]` 与 `params.navigation` 推导，消费者可以替换 section 或 taxonomy，模板不得恢复固定路径。
- 单篇文章是主 section 下的详情页，不作为重复的主导航入口；标签负责主题分类，归档负责按年份浏览，About 与其他导航项保持相同的字号和位置。
- 归档使用 Hugo page collection 的 `GroupByDate`，仅输出日期和标题，保持长文博客的低噪声目录。

## 6.5 内容系统与工程（2026-08 新增）

### 6.5.1 SEO 元数据（`head.html` 集中输出）

| 项 | 实现 |
|---|---|
| canonical | 每页 `.Permalink` |
| OpenGraph | title/type(article\|website)/url/image(双鱼logo+alt)/description/site_name/locale+alternate |
| Twitter Card | summary 卡 |
| JSON-LD | Person（含 sameAs 社交链接）+ 非首页 BreadcrumbList（站→section→文章） |
| description 兜底 | 页面 `.Description` → 共享 `summary-source.html` → `params.description` → `params.subtitle` → `site.Title`，plainify 后截 155 字符 |

### 6.5.2 可访问性（WCAG 2.x AA）

- skip link（body 首元素 → `#main-content`，聚焦滑入）
- 全局 `:focus-visible` 朱砂焦点环（outline 2px + 3px offset）
- 正文内联链接使用实色 `currentColor` 常驻下划线；抬升面链接同时切换到专用高对比角色，不只依赖颜色辨识
- `prefers-reduced-motion` 全动画降级（0.01ms）
- 主题切换按钮 `aria-pressed` + 三语 `aria-label`，透明命中区固定为 28×28px，在满足 WCAG 2.2 最小目标尺寸的同时维持移动端头部比例
- 首页诗句 Hero 使用原生 button，支持点击、触摸、Enter 与 Space

### 6.5.3 内容交互

- **标题锚点**：`_markup/render-heading.html` 构建期注入 `#`（hover 显示，朱砂），零 JS
- **返回键**：文章标题上方 `← 返回`（`CurrentSection`，三语 i18n）
- **链接策略**：`_markup/render-link.html` — 外链自动 `target="_blank" rel="noopener noreferrer"`；根相对站内内容链接经 `relLangURL` 保留项目子路径与语言段，锚点和普通相对链接不动
- **图片懒加载**：`_markup/render-image.html` — markdown 图片 `loading=lazy decoding=async`；手写 `<figure>` 不注入（LCP 首图保护）

### 6.5.4 404 页

「四〇四」朱砂大字 + 纸墨文案（"此页纸已散佚，笔墨未落。"）+ 首页/文章导航，三语 i18n。

### 6.5.5 摘要算法（`partials/summary.html`）

- **宽度感知截断**：CJK/全角=2 全宽单位（UTF-8 字节≥3 判定），ASCII/半角=1
- **长度**：140 全宽单位 ≈ 3 行（47 单位/行 × 3 行，43em 容器实测；基准 pianpker 121–123 单位/3 行）
- **来源优先级**：Hugo `.Summary`（`<!--more-->` → Front Matter `summary` → 自动摘要）→ `.Description` 兜底
- 超限追加 `…`；首页 tags 组/untagged 组/列表页三处统一调用

## 7. ADR 记录

### ADR-1: 配色方法论（OKLCH + Cohen-Or + WCAG + 742 点名）
- **状态**: 已接受
- **方法**: sRGB→OKLCH 感知空间（Ottosson 2020）；Cohen-Or 色相和谐模板（SIGGRAPH 2006，邻近≤30°/三角~120°/互补~180°）；WCAG 对比度目标驱动（"明度≠对比度"，二分搜索明度）；选色只从 742 中华传统色库点名，兜底（明度推移）必须标注
- **来源**: zhongguo-traditional-colors（harmony CSV 算法同源）、OKHST 实践（tenphi.me）

### ADR-2: 暗色模式选剑锋紫（已被 ADR-7 取代）
- **状态**: 已接受 → **已被 ADR-7 取代**（v2 改墨黑体系）
- **背景**: 用户原方案含剑锋紫/绿灰两组有色暗底。客观对比：潭水绿主色 1.05:1、黄昏灰强调 1.14:1、綟绶强调 2.52:1 全部结构性失败；剑锋紫 + 舌红 5.18:1 通过
- **决策**: 保留剑锋紫做暗底（有色暗底潮流 + 正文 10.39:1），角色全部按算法归位
- **取代原因（ADR-7）**: 用户实测反馈暗色“怪、两套设计” —— 剑锋紫 H=314.6° 与亮色暖纸温度轴分离；墨黑体系让亮暗同属纸墨二元

### ADR-7: 暗色改用墨黑体系（取代 ADR-2 剑锋紫）
- **状态**: 已接受
- **决策**: 暗底用墨色 `#1D1B1C`（742 库真名，恰为亮色正文墨色），月白 `#ECE6DC` 正文、朱红 `#ED5126` 链接
- **理由**: 亮=墨字在纸，暗=白墨在墨，双模式同一设计；暗底取亮色正文色，纸墨闭环叙事
- **实测**: 朱红 on 墨色 4.74 ✅（on #232323 4.35 ❌，故底必须取暗端）；月白 13.80 ✅；抬升面链接须用舌红 6.03 ✅

### ADR-3: 命名演化
- Ink-Shōwa（不符合）→ Kanso 簡素（撞名 bitgnostiq/kanso）→ **Inyo 陰陽**（纸墨二元性，查重通过）

### ADR-4: 独立仓库 + Hugo Module 分发
- **决策**: 主题独立仓库 `github.com/FeiNiaoBF/hugo-theme-inyo`，博客经 `module.imports` 引入；Vercel 要求模块仓库公开
- **理由**: 开源分享、内容与主题解耦、契合现有 Hugo Modules 架构

### ADR-5: 数学公式职责边界
- **决策**: 主题带 KaTeX 开关（`params.math`），博客侧的 goldmark passthrough 配置留在博客
- **理由**: 数学支持是站点需求不是主题需求，主题只提供能力

### ADR-8: Logo 改为双鱼太极 + 朱砂环（v4）
- **状态**: 已接受（2026-08-06 用户从 4 候选选定，几何经 2 轮修正确认）
- **背景**: 旧 Logo 为传统印章（朱砂底「陰陽」白字）。v3 对齐 pianpker 布局后，站名放大 36px 成为主角，印章信息过载；且 Inyo=陰陽，太极图形直表本义
- **候选对比**: A 太极章（朱砂框+太极）／B 双鱼太极（无框）／C 印章 v2（细线白文印）／D 印章太极（满底）
- **决策**: B 方案双鱼太极。**修正 1**（v2）: 手推弧线 sweep 方向画反不成太极 → 对照标准 Yin-Yang 路径修正；**修正 2**（v4）: 改用基本形状叠加（左半圆+上下小圆），数学验证面积对称 200π=200π，杜绝几何错误
- **配色**: 网页内联版圆盘/墨鱼/浅色鱼使用 `--seal-disc` / `--seal-ink` / `--seal-light`，朱砂环使用 `--cinnabar`，亮暗模式自适应；鱼眼经典对位（黑鱼白点/白鱼黑点）
- **favicon**: 无 CSS 变量上下文，继续使用品牌固定色版 `img/seal-yang.svg`（复用原文件名，内容已换）；这是明确记录的唯一固定品牌色例外
- **社交分享图**: 默认 `ogImage` 使用 `img/seal-yang-og.png`，避免社交平台对 SVG 支持不一致；该 PNG 是固定品牌色静态输出，不进入运行时 CSS token 系统

## 8. 首页诗句 Hero

首页 Hero 是固定高度的横向诗签，以阅读体感为先，显示居中双行的“诗句 + 作者出处”。诗句与出处各限制为单行省略，避免远程内容改变首页布局或造成横向溢出。

- **初始数据**：主题自带 `data/inyo/hero_poems.toml`，页面加载不发起网络请求
- **主动交互**：整条 Hero 是原生 button；点击、触摸、Enter、Space 均抽取下一句
- **远程增强**：`heroPoetry.api.enabled` 默认 `false`；开启后仅在主动交互时请求，响应必须规范化为 `text` 与 `source`
- **失败策略**：超时、CORS、非 2xx、字段缺失或连续重复时静默使用本地诗句，不显示加载或错误文案
- **页面范围**：数据和脚本仅输出到首页；文章、列表、标签、About 与 404 不包含 API 地址
- **颜色纪律**：正文使用 `--ink`，出处使用 `--camel`，墨线使用 `--cinnabar`；不新增颜色 token

## 9. 朱红双翼墨线与落字动效

动效只发生在 Hero 内部，采用两阶段反馈：点击后无需等待 API，两条朱红双翼路径立即从底部中央起笔，沿 Hero 底边、左右圆角和侧边上行，最后沿顶部边框在中央合墨；远程或本地诗句就绪后，旧句上移 6px 淡出，新句由下方 4px 进入并从 2px 模糊恢复清晰，出处延迟 40ms 落定。hover 只在 `hover:hover` 下改变边框，不请求数据。

- 双翼几何使用根据 Hero 实际尺寸同步的内联 SVG 圆角路径；仅保留左右两条主路径，通过 `stroke-dashoffset` 从底部中央沿边框向顶部中央绘制，并在完成后淡出；不添加第二层描边，也不循环播放
- 等待期间保留弱朱红边缘，不增加 loading 文案、图标或循环动画
- 文字动效只使用 `transform`、`opacity` 与小范围 `filter`
- Hero 高度保持桌面 68px、移动端 56px
- `prefers-reduced-motion: reduce` 下立即换句，不运行线条、位移或模糊动画
- 请求期间保留当前诗句并设置 `aria-busy="true"`；结束后通过 polite live region 宣布新内容

### ADR-9: 诗句 Hero（2026-08-08）
- **状态**: 已接受
- **决策**: 移除首页背景画与全屏效果，将品牌记忆点收敛为本地优先、可选远程的随机诗句和朱红双翼墨线
- **理由**: 诗句更贴合长文阅读与纸墨气质；局部动效比全屏画面更安静，且不需要图片处理与 hover-only 交互
- **接口**: 默认示例使用诗泉 `https://poetry.palemoky.com/api/poems/random`；接口地址可替换，主题离线时仍完整可用
- **可访问性**: 原生 button、动态 `aria-label`、`aria-busy`、live region、可见焦点环与 reduced-motion 降级

## 10. 致谢与来源

- 742 中华传统色: github.com/nevertoday/zhongguo-traditional-colors
- OKLCH: Björn Ottosson 2020；OKHST: tenphi.me
- 色相和谐: Cohen-Or et al., "Color Harmonization", SIGGRAPH 2006
- 排版实测参照: valaxy-theme-shuimo（已弃用其作为参考，保留其排版数据）
- 字体: 思源宋体/霞鹜文楷/更纱黑体（均为开源 OFL）
- 字体交付: 霞鹜文楷自托管分片来自官方 lxgw-wenkai-webfont 包（97×2 unicode-range 分片）；Windows 上 `cn-font-split` 因 koffi/libffi 不可用，改 vendor 官方分片
